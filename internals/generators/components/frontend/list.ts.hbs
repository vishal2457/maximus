import { Component } from '@angular/core';

@Component({
  selector: 'app-{{name}}-list',
  template: `<gb-grid-shell gridTitle="Customers" apiURL="/{{name}}/list">

      <gb-toolbar icon="add" name="Add" (handleClick)="add()" />

    <!-- columns -->
      {{#each schemaValue.properties}}
      <gb-column field="{{@key}}" alignment="left" />
      {{/each}}
    <!-- columns -->

    <!-- filters -->
    {{#each schemaValue.properties}}
      <gb-grid-filter label="{{propercase @key}}" field="{{@key}}" type="{{this.type}}" />
    {{/each}}
    <!-- filters -->

    <!-- actions -->
      <gb-action icon="edit" (handleClick)="edit($event)" tooltip="Edit" />
    <!-- actions -->
  </gb-grid-shell>`,
})
export class App{{properCase name}}ListComponent {
  
  add() {
    this.router.navigate(['/{{name}}/add']);
  }

  edit(e: any) {
    this.router.navigate(['/{{name}}/edit/' + e.cellData.id]);
  }
}
