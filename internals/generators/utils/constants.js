 const GENERATOR_CONSTANTS = {
    INTEGER: 'INTEGER',
    STRING: 'STRING',
    BOOLEAN: 'BOOLEAN',
    ENUM: 'ENUM',
    UNIQ: 'unique',
    PRIMARY: 'primary',
    DEFAULT_VALUE: 'defaultValue',
    WEB_FORM: {
        TEXT_AREA: 'textarea',
        SELECT: 'select',
        SKIP: 'skip'
    }
}

module.exports = GENERATOR_CONSTANTS